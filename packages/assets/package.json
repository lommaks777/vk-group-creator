{
  "name": "@vk-group-creator/assets",
  "version": "1.0.0",
  "description": "Image generation for VK group assets",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "sharp": "^0.33.0",
    "canvas": "^2.11.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/sharp": "^0.32.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.8"
  }
}
