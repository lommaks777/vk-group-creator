{
  "name": "@vk-group-creator/templates",
  "version": "1.0.0",
  "description": "Templates for VK group content",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/handlebars": "^4.1.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.8"
  }
}
